{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome", // Or "msedge" if you use Microsoft Edge
      "request": "launch",
      "name": "Vite: Attach to Chrome",
      "url": "http://localhost:5173", // Make sure this port matches your Vite server
      "webRoot": "${workspaceFolder}"
    }
  ]
}